import { Interfaces } from '../identifiers';
export default function markExperimentPoint(url: string, userId: string, experimentPoint: string, partitionId?: string): Promise<Interfaces.IMarkExperimentPoint>;
